# AGENTS.md — 给所有 AI Agent 的硬约束

本文件约束任何以 Agent 身份操作本仓库（`unified-video-tool`）的 AI（Claude Code / Codex / 其他 Coding Agent）。**请完整阅读并严格遵守**，违反视为事故。

## 1. 定位边界（先读）

本工具是**本地视频文案提取**：用户粘贴抖音/小红书/B站/YouTube 的**单个**视频/笔记链接 → 提取标题/文案/语音转写 → 存 Obsidian 或生成图文。只做「读」，不做「写」。

## 2. 禁止清单（❌ 具体到工具/动作）

- ❌ **任何平台写操作**：发帖、发评论、点赞、收藏、关注、私信、举报——无论通过 OpenCLI、浏览器还是 API。
- ❌ **批量/自动化抓取**：批量爬取用户列表、批量下载、刷量、采集粉丝/评论数据做商业用途。
- ❌ **浏览器自动化驱动**：selenium / playwright / puppeteer / webdriver / CDP，用于自动操作平台页面（登录、点击、滚动、表单提交）。
- ❌ **泄露/外传登录态**：OpenCLI 复用的是用户 Chrome 登录态（Cookie），**严禁**把 Cookie、登录态、本地 `.env` 里的 Key 写入日志、提交到 Git、或发往任何外部服务。
- ❌ **逆向平台私有接口做规模商用**：解析单条内容可以，但不得把解析器改造成批量爬虫或对外提供付费服务。

## 3. 允许清单（✅）

- ✅ 用户粘贴**单个**链接 → 提取该条内容的标题/正文/语音转写 → 存 Obsidian / 生成图文卡片 / 生成 HTML PPT。
- ✅ 用 OpenCLI 读 Chrome 登录态**只用于读取用户自己有权查看的内容**（小红书笔记、抖音视频页）。
- ✅ 本地离线转写（faster-whisper / sherpa-onnx）、内录系统声音（WASAPI loopback，用户自己打开播放页）。

## 4. 为什么（原因）

本项目的抖音/小红书解析**复用 Chrome 登录态**。任何「自动化写操作」（发帖/评论/点赞）或「批量抓取」都会被平台风控识别为机器人，**极容易封号**——封的是用户真实账号，不是工具。所以硬边界是：**只读单条、绝不写、绝不批量**。登录态和 Key 泄露则直接威胁账号与资金安全。

## 5. 触发词自查

若本仓库任何脚本/流程/文档/PR 中出现以下词，应视为违规，需移除/改正后再继续：

```
selenium  playwright  puppeteer  webdriver  cdp  headless
自动发帖  自动评论  自动点赞  刷量  批量爬取  批量下载  采集粉丝
cookie泄露  上传cookie  提交.env  打印apikey
```

一句话：**本项目只做「用户粘一条链接 → 读 → 转文字 → 存本地」，其余一律不做。**
