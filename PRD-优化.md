# Unified Video Tool 优化 PRD

> 目的：把 live-class-skill 的可复用优点 + 本项目的现状问题，整理成可勾选的优化清单。
> 用法：你在每条「选项」里挑一个（或整条勾选），把编号发我（例：「选 A1-b、A2-b、A3、B1」），我直接改代码跑通给你看。

## 现状关键发现（为什么值得做）

1. **faster-whisper 写死 `device='cpu'`**（`server.py:223` 和 `:235`）—— 你有 RTX 4070 但完全没用上，转写一直白慢 5–10 倍。
2. **每次转写都重新加载模型**（`transcribe_video` / `transcribe_audio_file` 各自 `new WhisperModel`），连续提取多个视频时重复加载，白耗几秒/次。
3. **只能处理有下载链接的视频** —— 直播、DRM 版权、网页内嵌视频抓不到。
4. 转写引擎是 `faster-whisper small`，`compute_type='int8'`，`language='zh'`，字幕优先、whisper 兜底（这套逻辑本身没问题，问题是速度）。

---

## A. 转写加速

### A1 — 转写切 GPU（P0，最值，零成本）

- **问题**：`device='cpu'` 写死，4070 闲置，转写慢。
- **选项**：
  - `A1-a` 直接改 `device='cuda', compute_type='float16'`（最简单；但换到无 GPU 的机器会崩）
  - `A1-b` 移植 live-class-skill 的 `cuda_rt.py` 只读探测：有 GPU + 可复用 CUDA 库 → 自动 GPU，没有 → 静默降 CPU（**推荐**，开箱即用、不崩）
- **优势**：转写快 5–10 倍；CUDA 运行库你机器上已有（`D:\mini\Lib\site-packages\torch\lib`），零下载零成本。

### A2 — 引入 sherpa-onnx 中文快速转写

- **问题**：抖音/小红书/B站文案 90% 是普通话，`faster-whisper small` 相对慢（1–2 倍实时 CPU）。
- **选项**：
  - `A2-a` 中文短内容一律走 sherpa（已装好在 `D:\live-class-skill`，实测 **15 倍实时**）
  - `A2-b` 双引擎分派：中文 → sherpa（秒级）；英文/中英混读/长视频 → faster-whisper（准确率优先）（**推荐**）
  - `A2-c` 暂不引入，先做 A1 看 GPU 效果够不够用
- **优势**：中文场景快一个量级；sherpa 中文模型（370MB）+ CUDA 版引擎**你机器上已经装好了**，复用即可。
- **注意（短板）**：sherpa 是纯中文普通话模型，英文/方言/中英混读会明显弱，所以要用 `A2-b` 分派，不能一刀切替换。

### A3 — 模型复用（缓存 WhisperModel 实例）

- **问题**：`transcribe_video` 和 `transcribe_audio_file` 每次调用都重新加载模型。
- **选项**：`A3-做`（模块级单例懒加载，首次加载后常驻）/ `A3-不做`
- **优势**：连续提取多个视频省去重复加载（每次几秒）；配合 A1 一起做收益更明显。

---

## B. 新增能力

### B1 — WASAPI loopback 内录兜底（覆盖「抓不到的」）

- **问题**：直播、DRM 版权内容、网页内嵌视频，yt-dlp 抓不到，现在直接失败。
- **选项**：`B1-做` / `B1-不做`
- **做法**：搬 live-class-skill 的 `_wasapi.py`（纯 ctypes 调系统 Core Audio，零第三方），在「链接解析/下载失败」时提供「内录模式」——录系统正在播放的声音 → 走现有转写。打开播放页由用户手动完成（防封号，不驱动浏览器）。
- **优势**：把工具从「只能转有链接的视频」扩展到「系统能出声的都能转」；直播代听、不可下载内容全覆盖。

---

## C. 工程规范（锦上添花）

### C1 — outputs 落盘规范化（单一可信源）

- **问题**：`outputs/` 平铺，一场多次提取会混，难清理、难追溯。
- **选项**：`C1-做` / `C1-不做`
- **做法**：学 live-class-skill 的 `session/<平台>_<标题>_<时间>/` 目录 + `meta.json`（记录 url/平台/标题/时间），一次提取一个目录。
- **优势**：好清理、可追溯、后续加「历史记录」功能也容易。

### C2 — AGENTS.md 硬约束（防封号边界）

- **问题**：项目用 OpenCLI 读 Chrome Cookie 登录态（小红书），有封号风险，但没有任何「给 AI 的边界约束」。
- **选项**：`C2-做` / `C2-不做`
- **做法**：写一份 `AGENTS.md`，五段式：定位边界 → 禁止清单（具体到工具名）→ 允许清单 → 原因 → 触发词自查。
- **优势**：防止未来 AI 改代码时误加「自动发帖/评论/驱动浏览器」等封号操作。

---

## 建议优先级

| 级别 | 编号 | 一句话 |
|------|------|--------|
| P0 | A1-b | 切 GPU，立竿见影快 5–10 倍，零成本 |
| P1 | A3 | 模型复用，顺手做 |
| P1 | A2-b | 中文转写再快一个量级（sherpa 已就绪） |
| P2 | B1 | 新增内录能力，覆盖直播/DRM |
| P2 | C1 / C2 | 落盘规范 + 防封号边界 |

**最小改动、最大收益**：先做 `A1-b + A3`（两个都是改 `server.py` 转写相关几十行，不动架构）。
